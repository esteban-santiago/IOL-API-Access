// assert = require('assert');
// expect = require('chai').expect;
// should = require('chai').should();
// //let AuthClient = require('../dao/auth_client');

// let { COME } = require('../models/stocks/come');
// let { MELI } = require('../models/stocks/meli');
// let { StockService } = require('../services/stock_service');

// let come = new StockService(new COME());
// let meli = new StockService(new MELI());

// //let auth = new AuthClient(); 

 
// describe('Stock COME', function () {
//     describe('#getTicker()', () => {
//         it('should return COME Stock ticker', () => {
//             console.log( come.getProperties() );
//             //come.getProperties().should.to.have.length.above(0);
//         });
//     });
// });

// describe('Stock MELI', function () {
//     describe('#getTicker()', () => {
//         it('should return MELI Stock ticker', () => {
//             //meli.getProperties().should.to.have.length.above(0);
//         });
//     });
// });
